# 1pm 功能测试清单

本文档覆盖 1pm（1Panel 多机网关）全部功能点与可操作验证步骤。  
测试时请准备 **至少两台 Linux 机器**（Master / Agent 角色互斥，禁止同机双装）。

---

## 0. 测试环境约定

### 0.1 机器与账号

| 角色 | 建议 | 说明 |
|------|------|------|
| Master | 已安装并初始化 1Panel 社区版 | 记录面板端口、安全入口、账号密码 |
| Agent | 另一台已安装并初始化 1Panel | 账号密码需在安装时写入（供自动登录） |
| 客户端 | 浏览器（建议 Chrome/Firefox） | 用于登录、切节点、管理页操作 |
| 可选 CDN | Mac/CI 上 Python 静态目录模拟 Release | 离线/内网安装验证用 |

示例（可按实际替换）：

```text
Master IP:  <MASTER_IP>
Agent IP:   <AGENT_IP>
SSH 用户:   parallels（或 root）
1Panel 账号/密码: <PANEL_USER> / <PANEL_PASS>
安全入口:   <ENTRANCE>（如 tomas）
面板端口:   <PORT>（如 52045）
```

### 0.2 通用检查命令

```bash
# 版本与服务
1pm version
systemctl status 1pm-master.service   # Master
systemctl status 1pm-agent.service    # Agent
journalctl -u 1pm-master.service -n 50 --no-pager
journalctl -u 1pm-agent.service -n 50 --no-pager

# 端口与证书
ss -lntp | grep -E '<PORT>|62045'   # Master 接管后内部口多为 原端口+10000
ls -la "$(grep '^BASE_DIR=' /usr/local/bin/1pctl | cut -d= -f2)/1panel/secret/" 2>/dev/null || true

# 配置
sudo cat /var/lib/1pm/master.json                 # Master
sudo cat /root/.1panel-agent/agent.json           # Agent
```

### 0.3 通过标准

- **通过**：预期行为全部出现，无报错或报错可解释且已恢复。
- **失败**：核心路径不可用、数据损坏、角色互斥被突破、旧会话泄露到错误节点。

---

## 1. 安装与卸载

### 1.1 Master 安装（CDN / Release）

**目的**：验证 `install.sh` 下载、校验、接管端口、生成 Token、拉起 systemd。

**步骤**：

1. Master 上确认仅有 1Panel，无旧 1pm：`command -v 1pm && 1pm uninstall || true`
2. 记录安装前面板端口：`ss -lntp | grep 1panel` 或浏览器访问 `http://<MASTER_IP>:<PORT>/<ENTRANCE>`
3. 执行安装（示例，内网 CDN）：

```bash
export INSTALL_CDN=global VERSION=<tag>
export GITHUB_API=http://<CDN_HOST>:8765
export GITHUB_DL=http://<CDN_HOST>:8765
curl -fsSL "$GITHUB_DL/AnkioTomas/1panel-agent/releases/download/$VERSION/install.sh" | sudo bash
```

4. 检查：

| 检查项 | 期望 |
|--------|------|
| `systemctl is-active 1pm-master` | `active` |
| `1pm version` | 与安装 tag 一致 |
| 公网 `<PORT>` | 由 `1pm` 监听 |
| 内部口（通常 +10000） | `1panel-core` 监听 `127.0.0.1` 或本机 |
| `/var/lib/1pm/master.json` | 含 `token`、`original_port`、`internal_port` |
| 浏览器登录本机面板 | 仍可打开 `/<ENTRANCE>` |

**失败场景**：

- 无 1Panel / 非 root → 安装脚本应失败并说明原因。
- checksum 失败 → 不得安装脏二进制。

---

### 1.2 Agent 安装（Master 签名 curl）

**目的**：验证 HMAC 安装命令、`PANEL_PASS`、systemd、注册上线。

**步骤**：

1. Master 管理页复制安装命令，或本地签发：

```bash
TOKEN=$(sudo jq -r .token /var/lib/1pm/master.json)
TS=$(date +%s)
SIGN=$(printf 'timestamp=%s' "$TS" | openssl dgst -sha256 -hmac "$TOKEN" | awk '{print $NF}')
# HTTP 示例；若 Master 已开 SSL，用 https 且 curl -k
curl -fsSL "http://<MASTER_IP>:<PORT>/agent.sh?timestamp=$TS&sign=$SIGN&name=agent-e2e&group=lab" \
  | sudo PANEL_PASS='<PANEL_PASS>' bash
```

2. 检查 Agent：

| 检查项 | 期望 |
|--------|------|
| `1pm-agent` active | 是 |
| `agent.json` | `master`、`token`、`name`、`group`、加密密码 |
| Master 日志 | `agent online: … name=… group=…` |
| 管理页在线表 | 出现该节点 |

**失败场景**：

- Agent 上已有 Master → 脚本拒绝「不能同时作为 agent」。
- `PANEL_PASS` 错误 → `setpwd` 失败，安装中止。
- 签名过期（>5 分钟）或错误 → `/agent.sh` 401。

---

### 1.3 角色互斥

| 操作 | 期望 |
|------|------|
| Master 机再跑 Agent 安装脚本 | 失败 |
| Agent 机再装 Master | 失败 |
| `1pm master` 在已装 Agent 的机器 | 启动拒绝 |
| `1pm agent run` 在已装 Master 的机器 | 启动拒绝 |

---

### 1.4 卸载

**Master**：

```bash
sudo 1pm uninstall
```

| 检查项 | 期望 |
|--------|------|
| `1pm-master` | 不存在 / inactive |
| 面板端口 | 回到 1Panel 直接监听原公网口 |
| `/var/lib/1pm` | 清理 |
| `/usr/local/bin/1pm` | 删除（若无另一角色） |

**Agent**：

```bash
sudo 1pm uninstall
```

| 检查项 | 期望 |
|--------|------|
| `1pm-agent` | 清除 |
| `~/.1panel-agent` 或 `/root/.1panel-agent` | 清除 |
| Master 侧 | 节点离线（不再出现在在线表） |

---

## 2. Master 端口接管与本机反代

### 2.1 接管行为

| 检查项 | 期望 |
|--------|------|
| 对外仍用原端口访问面板 | 成功 |
| 内部端口仅本机/内网 | `1panel-core` 不在原公网口 |
| Master 重启后 | 仍保持接管，不必手工改端口 |

### 2.2 本机面板可用性

1. 浏览器打开 `http(s)://<MASTER_IP>:<PORT>/<ENTRANCE>`
2. 登录 1Panel
3. 正常浏览首页 / 设置页

**期望**：与未装 1pm 时行为一致（除侧栏多了节点切换）。

---

## 3. 鉴权与管理页门禁

### 3.1 未登录访问 `/__mp/`

1. 无面板会话的浏览器 / `curl` 访问 `http://<MASTER_IP>:<PORT>/__mp/`

**期望**：重定向到主节点登录（含安全入口），**不是**裸露管理页。

### 3.2 已登录访问

1. 先登录本机 1Panel
2. 打开 `/__mp/` 或侧栏「节点管理」

**期望**：管理页 200，可见安装命令、在线节点、节点管理卡片。

### 3.3 `/__mp/touch`

| 状态 | 期望 |
|------|------|
| 已登录 | `204` |
| 未登录 | `401` |

### 3.4 登录页强制回主节点

1. 切到子节点（见第 5 节）
2. 访问登录相关路径或被踢到登录

**期望**：清掉 `mp_node`，回到主节点登录入口（不卡在子机登录页）。

---

## 4. 节点管理页（`/__mp/`）UI

### 4.1 文案与分区

勾选页面是否包含：

- [ ] 标题「节点管理」
- [ ] 子节点安装命令（名称 / 分组 / 复制 / 轮换 Token）
- [ ] 在线节点表（状态、名称、IP、Agent 版本、1Panel 版本、CPU、内存、进入面板）
- [ ] 「节点管理」卡片状态行：当前节点、1pm 版本、1Panel 版本、上游、切换回主节点
- [ ] 「1panel-Agent 管理面板」+ 更新主/子 1pm
- [ ] 「1Panel 更新」+ 更新主/子 1Panel
- [ ] 「1Panel SSL」+ 开启/关闭**主节点** SSL

### 4.2 安装命令

| 步骤 | 期望 |
|------|------|
| 填名称/分组后点「复制命令」 | 剪贴板为新签名 URL，含 `name`/`group` |
| 约 5 分钟后旧链接 | `/agent.sh` 返回 401 |
| 「轮换 Token」 | 确认后旧 Agent 全部掉线；新命令可用；需重新安装 Agent |

### 4.3 在线表刷新

1. 打开管理页，观察约 5 秒刷新
2. Agent 上 `stress` 或闲置，看 CPU/内存变化

**期望**：主节点 + 在线 Agent 均有数据；Agent 断开后从列表消失或 offline。

### 4.4 明暗主题

跟随系统 / 1Panel `GlobalState.theme`：管理页 light/dark 可读，无对比度崩溃。

---

## 5. 节点切换与会话

### 5.1 侧栏切换

1. 登录主节点面板，侧栏底部应有节点切换控件
2. 下拉可见：主节点、在线 Agent（按分组）、「节点管理」、退出登录

| 操作 | 期望 |
|------|------|
| 点某 Agent | Cookie `mp_node` 写入，进入该节点面板 |
| 点主节点 / 「切换回主节点」 | 清 `mp_node`，回本机面板 |
| 点「节点管理」 | 进入 `/__mp/` |

### 5.2 进入子节点自动登录

1. Agent 已正确 `setpwd`
2. 从管理页或侧栏进入子节点

**期望**：

- 无需再输入子机密码即可进面板
- 浏览器 Cookie 变为子机会话
- 子机页面可用（站点、文件等基础浏览）

**失败**：密码错误 / 未 setpwd → 应看到登录失败日志，不应把主机会话写到子机。

### 5.3 切回主节点恢复会话

1. 主节点登录后切到子节点
2. 再切回主节点

**期望**：本机面板会话从 Master 内存恢复，一般无需重新登录（会话未过期时）。

### 5.4 Agent 离线时的切换

1. 停掉 Agent：`systemctl stop 1pm-agent`
2. 仍带旧 `mp_node` 访问根路径

**期望**：丢掉过期节点选择，回主节点（不无限挂死）。

### 5.5 WebSocket 隧道

在子节点面板打开依赖 WS 的页面（终端、日志流等，视 1Panel 版本）。

**期望**：功能可用，连接经 Master 隧道，不直连 Agent 公网端口。

---

## 6. Agent 本机面板硬化（贯穿始终）

> 正确模型：Agent 本机面板 **不对外、不开 SSL**；隧道加密只靠 Master HTTPS + Agent `wss`。

### 6.1 安装后立即检查

Agent 安装并 `setpwd` / 首次 `agent run` 后：

```bash
sudo sqlite3 /opt/1panel/db/core.db \
  "SELECT key,value FROM settings WHERE key IN ('SSL','BindAddress');"
# 或对应 BASE_DIR 下的 db
ss -lntp | grep <AGENT_PANEL_PORT>
ls "$(grep '^BASE_DIR=' /usr/local/bin/1pctl | cut -d= -f2)/1panel/secret/server.crt" 2>&1
```

| 检查项 | 期望 |
|--------|------|
| `SSL` | `Disable` |
| `BindAddress` | `127.0.0.1` |
| `ss` 监听 | `127.0.0.1:<port>`，**不是** `0.0.0.0` |
| `server.crt` | 不存在 |
| `agent.json` → `panel_url` | `http://127.0.0.1:<port>` |

从 Agent 机器外网 IP:端口访问面板应 **失败**；本机 `curl http://127.0.0.1:<port>/<ENTRANCE>` 成功。

### 6.2 重启复查

```bash
# 故意破坏后再重启，验证会纠偏
# （可选）在面板里改回 0.0.0.0 / 开启 SSL 后：
sudo systemctl restart 1pm-agent
journalctl -u 1pm-agent -n 30 --no-pager | grep hardening
```

**期望**：日志出现 hardening；再次检查仍为 `127.0.0.1` + SSL Disable。

### 6.3 与 Master SSL 无关

主节点开关 SSL **不得**把 Agent 本机 SSL 打开。  
验证：主节点开 SSL 后，Agent 仍 `SSL=Disable` 且只听 `127.0.0.1`。

---

## 7. Master 面板 SSL 与隧道加密

### 7.1 开启主节点 SSL

**前置**：管理页已登录；Agent 在线。

**步骤**：

1. `/__mp/` →「开启主节点 SSL」并确认
2. 等待面板短暂重启（约数十秒）

| 检查项 | 期望 |
|--------|------|
| Master：`{BASE_DIR}/1panel/secret/server.crt` | 存在 |
| `curl -sI http://<MASTER_IP>:<PORT>/tomas` | `307` → `https://…` |
| `curl -skI https://<MASTER_IP>:<PORT>/tomas` | `200` |
| Master 日志 | `panel tls cert loaded`、`listening mux http/https`、上游 `https://127.0.0.1:<internal>` |
| Agent `agent.json` | `master_tls: true` |
| Agent 日志 | `connected to master`（wss） |
| 管理页安装命令 | `https://…` 且 `curl -k`、脚本含 `--master-tls` |

### 7.2 关闭主节点 SSL

1. 「关闭主节点 SSL」
2. 等待证书删除与服务恢复

| 检查项 | 期望 |
|--------|------|
| 证书文件 | 删除 |
| HTTP | 直接 `200`，无强制 HTTPS |
| Agent | `master_tls` 清除/false，改回 `ws` 仍在线 |

### 7.3 混合架构

| 组合 | 期望 |
|------|------|
| Master 开 SSL，Agent 本机不开（默认） | **支持且推荐** |
| Master 不开 SSL，Agent `master_tls=false` | 明文 `ws`，可用 |
| Master 开 SSL，Agent 仍 `master_tls=false` | 应掉线或被 307/证书问题卡住 → 需同步/重装 |
| 仅 Agent 本机开 SSL | **不符合产品契约**；重启 Agent 应被硬化关掉 |

### 7.4 BASE_DIR 非默认

若 1Panel 安装时改过 `BASE_DIR`（非 `/opt`）：

1. `grep '^BASE_DIR=' /usr/local/bin/1pctl`
2. 开主节点 SSL 后证书应在 `$BASE_DIR/1panel/secret/`
3. Master 日志 `panel tls cert loaded from …` 路径与之一致

---

## 8. 更新能力

### 8.1 更新主节点 1pm

管理页「更新主节点 1pm」或：

```bash
# 需已登录面板会话；或在已鉴权环境下调 API
curl -sk -b '…cookies…' -X POST https://<MASTER_IP>:<PORT>/__mp/api/update-master
```

| 检查项 | 期望 |
|--------|------|
| 下载 Release / CDN | 成功 |
| `1pm version` | 变为新 tag |
| `1pm-master` | 自动重启后 active |
| 面板仍可访问 | 是 |

### 8.2 更新子节点 1pm

「更新子节点 1pm」（`force-update`）：

| 检查项 | 期望 |
|--------|------|
| Agent 从 Master 拉 `/agent.bin` | 成功 |
| Agent `1pm version` | 与 Master 一致 |
| 服务重启后重新上线 | 是 |

**顺序建议**：先更新 Master，再更新 Agent（Agent 吃的是 Master 磁盘上的二进制）。

### 8.3 更新主节点 1Panel

「更新主节点 1Panel」：

| 情况 | 期望 |
|------|------|
| 已是最新 | `skipped: true` |
| 有新版本 | 触发官方升级 API，面板重启后版本变化 |

### 8.4 更新子节点 1Panel

「更新子节点 1Panel」：

| 检查项 | 期望 |
|--------|------|
| Agent 自动登录本机面板 | 成功 |
| 已最新 | `skipped` |
| 有更新 | 子机面板升级流程启动；结果在 API `results` 中汇总 |

---

## 9. HMAC / 下载鉴权

### 9.1 合法签名

```bash
TOKEN=…; TS=$(date +%s)
SIGN=$(printf 'timestamp=%s' "$TS" | openssl dgst -sha256 -hmac "$TOKEN" | awk '{print $NF}')
curl -fsS -o /dev/null -w '%{http_code}\n' \
  "http://<MASTER_IP>:<PORT>/agent.bin?timestamp=$TS&sign=$SIGN"
```

**期望**：`200`。

### 9.2 非法 / 过期

| 请求 | 期望 |
|------|------|
| 无 query | 401 |
| `token=<明文>` | 401（禁止裸 token） |
| `timestamp` 偏差 >5 分钟 | 401 |
| 错误 `sign` | 401 |

---

## 10. CLI 命令

在对应角色机器上执行：

| 命令 | 期望 |
|------|------|
| `1pm version` / `-v` / `--version` | 打印构建版本 |
| `1pm help` | 用法说明 |
| `1pm master` | 前台启动 Master（一般用 systemd） |
| `1pm agent install <host:port> <token> --name n --group g` | 写配置，不挂长连接 |
| `1pm agent setpwd --password '…'` | 校验密码后加密存储 |
| `1pm agent run` | 连接 Master |
| `1pm uninstall` | 按本机角色清理 |

---

## 11. 安全与边界

### 11.1 Token 轮换

1. 管理页轮换 Token  
2. 旧 Agent：应全部无法保持连接  
3. 新安装命令：可装新 Agent  
4. 旧机器：需用新命令重装

### 11.2 密码存储

```bash
sudo grep -E 'panel_password[^_]|password_enc' /root/.1panel-agent/agent.json
```

**期望**：无明文 `panel_password`；仅有 `panel_password_enc`；存在 `secret.key`（权限 600）。

### 11.3 管理 API 未授权

未登录直接 `curl`：

- `GET /__mp/api/agents` → 401  
- `POST /__mp/api/rotate-token` → 401  
- `POST /__mp/api/panel-ssl` → 401  

### 11.4 同机双角色回归

任何路径都不能让同一台机器同时存在可用的 Master 与 Agent 服务。

---

## 12. 推荐回归顺序（冒烟 + 全量）

### 12.1 冒烟（约 20 分钟）

1. 两台卸干净 → 装 Master → 装 Agent → 上线  
2. 登录 → 管理页 → 切子节点 → 切回主节点  
3. 开主节点 SSL → Agent `master_tls` + HTTPS 访问 → 关 SSL  
4. 确认 Agent 始终 `127.0.0.1` + SSL Disable  

### 12.2 全量（约 1～2 小时）

按本文第 1～11 节逐项打勾；重点补：

- Token 轮换与重装  
- 1pm / 1Panel 四类更新  
- HMAC 正反例  
- BASE_DIR 非 `/opt`（若有环境）  
- Agent 故意改绑定/SSL 后重启纠偏  
- WS 类面板功能  
- 卸载后端口与面板恢复  

---

## 13. 记录模板

```text
日期:
版本 tag / commit:
Master IP / Agent IP:
BASE_DIR:
是否开过主节点 SSL:

[ ] 1 安装卸载
[ ] 2 端口接管
[ ] 3 鉴权门禁
[ ] 4 管理页 UI
[ ] 5 节点切换与会话
[ ] 6 Agent 硬化
[ ] 7 Master SSL / wss
[ ] 8 更新
[ ] 9 HMAC
[ ] 10 CLI
[ ] 11 安全边界

失败项与日志摘要:
```

---

## 14. 架构对照（测的时候别测偏）

```text
浏览器 ──HTTPS──▶ Master 公网口（可选，面板 SSL）
Agent   ──wss───▶ Master 公网口（Master 开 SSL 时必须）
Agent   ──http──▶ 127.0.0.1:本机面板（强制，永不对外、不开 SSL）
```

不要把「给 Agent 本机面板开 SSL」当成功能点；那是错误方向，回归时应验证其**不会发生**且会被硬化纠正。
